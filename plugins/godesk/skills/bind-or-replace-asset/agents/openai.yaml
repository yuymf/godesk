interface:
  display_name: "Bind or replace an asset"
  short_description: "Swap traceable prototype art without changing rules"
  default_prompt: "Use $bind-or-replace-asset to bind this image and preserve its provenance."
