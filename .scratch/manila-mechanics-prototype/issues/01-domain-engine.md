# Build the event-driven voyage engine

Type: task
Status: resolved

Implement validated placement, deterministic dice actions, resolution, action replay, and bot choices as framework-independent TypeScript.

## Answer

Implemented in `src/domain/game.ts`, covered by deterministic domain tests in `src/domain/game.test.ts`.
